# Conexão Postgres (banco vigilancia)
DATABASE_URL=postgresql://usuario:senha@host:5432/vigilancia

# Chave para JWT (use valor forte em produção)
JWT_SECRET=altere-em-producao

# Chave para criar primeiro admin via POST /api/admin/seed
SEED_SECRET=chave-seed-admin

# Webhook CADU: ao fazer upload do CADU na Manutenção, uma cópia do CSV é enviada em POST (multipart, campo "file") para esta URL (ex.: N8N).
# WEBHOOK_EXTRACAO_CADU_URL=https://vigilancia.fabricadosdados.ia.br/webhook/extracao-cadu
