# =============================================================================
# Variáveis para EasyPanel – Vigilância Web
# Copie este arquivo para as variáveis do serviço ou use como referência.
# =============================================================================

# Obrigatório: conexão Postgres (banco vigilancia)
DATABASE_URL=postgresql://usuario:senha@host:5432/vigilancia

# Obrigatório: chave para JWT (use valor forte em produção)
JWT_SECRET=altere-em-producao-uma-chave-longa-e-segura

# Opcional: chave para criar primeiro admin via POST /api/admin/seed
# Se definido, permite criar usuário admin com: POST /api/admin/seed
# body: { "secret": "<SEED_SECRET>", "email": "admin@...", "password": "..." }
SEED_SECRET=chave-seed-admin

# Opcional: webhook para envio do CSV do CADU (ex.: N8N). Ao fazer upload do CADU na Manutenção, uma cópia do arquivo é enviada em POST (multipart, campo "file") para esta URL.
WEBHOOK_EXTRACAO_CADU_URL=https://vigilancia.fabricadosdados.ia.br/webhook/extracao-cadu
